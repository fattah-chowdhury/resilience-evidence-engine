# Flood evidence

Run `ree run --config configs/examples/bangladesh_excerpt.yml` from the repository root.
This reuses the existing NASA excerpt, with unknown event date and ambiguous Sylhet locations.
It is an offline evidence example, not live flood monitoring. Source suitability:
`ree discover --config configs/examples/bangladesh_excerpt.yml`.
